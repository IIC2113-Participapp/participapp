<%= form_for @category, html: { class: 'form-inline center-align' } do |f| %>
  <fieldset>
    <legend><%= title %></legend>

    <div class="form-group center-align flex-wrap">
      <%= f.label :name, 'Nombre:', class: 'control-label' %>
      <%= f.text_field :name, class: 'form-control' %>
      <%= f.submit '¡Listo!', class: 'btn btn-success' %>
    </div>
  </fieldset>
<% end %>
